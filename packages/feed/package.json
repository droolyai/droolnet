{
  "name": "@wokenet/feed",
  "version": "0.1.0",
  "private": false,
  "description": "Deterministic, user-owned feed ranking with reproducible receipts for WOKE.NET clients",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "exports": { ".": { "types": "./dist/src/index.d.ts", "import": "./dist/src/index.js" } },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist coverage",
    "lint": "eslint src test",
    "test": "vitest run --exclude 'dist/**'",
    "test:coverage": "vitest run --coverage --exclude 'dist/**'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": { "vitest": "4.1.10" }
}
